node_modules/
dist/
.netlify/
.DS_Store

# Paid ebooks are never committed — this repository is public.
# They live in private storage and are served through an entitlement check.
ebooks/*.pdf

# Paid question banks. Public repo — these live in a private bucket.
content/*/bank.json
