# Built from the monorepo root so packages/app-core is available to copy in.
[build]
  base    = "/"
  command = "pnpm run build:packages && pnpm --filter casebook-lbsw build"
  publish = "apps/casebook-lbsw/dist"

[build.environment]
  NODE_VERSION = "22"
